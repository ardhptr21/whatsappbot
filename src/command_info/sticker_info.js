export default function candaan_info() {
  return `======= INFO COMMAND =======

*<gambar> !sticker*

Akan mengubah gambar menjadi sticker.
Untuk menggunakan ini, silahkan kirim gambar dengan caption *!sticker*

===========================
`;
}
